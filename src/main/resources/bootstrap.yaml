spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/finrem
      aliases:
        finrem.idam-secret: OAUTH2_CLIENT_FINREM
        finrem.finrem-doc-s2s-auth-secret: AUTH_PROVIDER_SERVICE_CLIENT_KEY
        finrem.docmosis-api-key: PDF_SERVICE_ACCESS_KEY
        finrem.docmosis-endpoint: PDF_SERVICE_BASEURL
